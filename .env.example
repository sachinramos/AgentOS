# Database — required
DATABASE_URL=postgresql://agentos:changeme@localhost:5432/agentos

# Session — required (generate a random string: openssl rand -hex 32)
SESSION_SECRET=

# Agent API key encryption — recommended (generate: openssl rand -hex 32)
AGENT_KEY_SECRET=

# Port (default 5000)
PORT=5000
